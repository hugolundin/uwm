#include <iostream>
#include <thread>
#include <chrono>
#include <unistd.h>

int main(int argc, char const *argv[])
{
    // std::thread server_thread(server_cb, "");
    // std::thread client_thread(client_cb, "");

    // server_thread.join();
    // client_thread.join();

    return 0;
}
